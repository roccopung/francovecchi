{
  "mcpServers": {
    "svelte": {
      "command": "npx",
      "args": ["-y", "@sveltejs/mcp"]
    },
    "Sanity": {
      "env": {},
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://mcp.sanity.io/",
        "--transport",
        "http-only"
      ]
    }
  }
}
